---
description: Fork the current OpenCode session to another machine (or another harness).
---

Run forklift. With no arguments it sends the latest session; pass `receive <token> [<harness>]` to fetch and resume a fork on this machine.

!`$HOME/.local/bin/forklift $ARGUMENTS`
